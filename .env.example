# Experiment Decision Log — environment template
#
# This MVP runs 100% client-side with localStorage.
# No API keys or secrets are required for local demo or Vercel deploy.
#
# Optional (only if you extend the project with a backend later):
# DATABASE_URL=
# ANALYTICS_WRITE_KEY=

NEXT_PUBLIC_APP_NAME=Experiment Decision Log
